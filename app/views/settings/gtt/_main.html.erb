<% checklist_tabs = []
  checklist_tabs.push({:name => 'general',
                       :partial => 'settings/gtt/general',
                       :label => :label_tab_general})
  checklist_tabs.push({:name => 'map',
                       :partial => 'settings/gtt/map',
                       :label => :label_tab_map})
  checklist_tabs.push({:name => 'geocoder',
                       :partial => 'settings/gtt/geocoder',
                       :label => :label_tab_geocoder})
%>

<%= render_tabs checklist_tabs %>

<% html_title(l(:label_settings), l(:label_gtt)) -%>
